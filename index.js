// Function Expression *Divide*

function divide (a = "2000",b = "100") {

    return a / b ;
}

// Arrow function *Square*

const square = parS => parS ** 2;

// Arrow function *Add*

const add = (parA, parB) => {return parA + parB; } 
